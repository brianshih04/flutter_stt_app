name: flutter_stt_app
description: "Multi-language Speech-to-Text application with BLoC pattern"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Speech Recognition — uses native Android Platform Channel (no plugin)

  # Translation
  google_mlkit_translation: ^0.10.0

  # Permissions
  permission_handler: ^11.0.1

  # State Management
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5

  # UI
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
